/**
 * Funciones
 * Autor: Sergio Andres Leguizamo
 * Fecha: jueves 04 de abril de 2024
 * 
 */
function calNum(pcontador,pnumero){
    let contador = pcontador;
    let numero = pnumero;
    while(contador<numero){
        contador++;
        console.log(contador);
    }
    
}

/**
 * constante 
 */
const calNum2=function(pcontador,pnumero){
    let contador = pcontador;
    let numero = pnumero;
    while(contador<numero){
        contador++;
        console.log(contador);
    }
    
}